import React from 'react';
import styled from 'styled-components';


const HeroContainer = styled.section`
  height: 90vh;
`;

const Hero = () => (
  <HeroContainer>
    <span>Hero</span>
  </HeroContainer>
);


export default Hero;
